{
	"name": "pi-monk",
	"version": "1.0.0",
	"description": "Monk Pi — any-language→EN + AAAK+ compression. 0-cost token austerity.",
	"type": "module",
	"scripts": {
		"test": "node --experimental-strip-types --test test/*.test.ts",
		"lint": "npx @biomejs/biome check --write",
		"lint:check": "npx @biomejs/biome check"
	},
	"pi": {
		"extensions": [
			"./index.ts"
		]
	}
}
