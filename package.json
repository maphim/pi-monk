{
	"name": "pi-monk",
	"version": "1.0.0",
	"description": "🧘 pi coding agent extension — AAAK compress + Token Austerity Directives. 0 API cost, 0ms latency.",
	"type": "module",
	"scripts": {
		"test": "node --experimental-strip-types --test test/*.test.ts",
		"lint": "npx @biomejs/biome check --write",
		"lint:check": "npx @biomejs/biome check"
	},
	"pi": {
		"extensions": [
			"./index.ts"
		]
	}
}
